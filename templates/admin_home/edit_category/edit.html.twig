{% extends 'admin_home/base.html.twig' %}

{% block title %}Modifier cette Catégorie{% endblock %}

{% block body %}
<h1>Modifier cette Catégorie | {{ category.name }}</h1>

    {{ form_start(form) }}
        {{ form_row(form.name) }}
        <button type="submit" class="btn btn-outline-primary">Enrégistrer</button>
    {{ form_end(form) }}
{% endblock %}
